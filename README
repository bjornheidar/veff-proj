Author: Björn Heiðar Rúnarsson

Project 3 in the course Web services at RU

To run:
pip install from requirements.txt

Run Kodemon.py, it will start both the UDP server and the API server

KodemonUDPServer listens by default on localhost:4000

KodemonAPI listens by default on localhost:4000
API routes are as follows:
/  				==> returns the index page
/api/v1/keys 	==> returns all the distinct keys that have been used
/api/v1/executiontime/key=key[&start_date=<int:timestamp>[&end_date=<int:timestamp]]
